import { FilterPlayrsPipe } from './filter-playrs.pipe';

describe('FilterPlayrsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlayrsPipe();
    expect(pipe).toBeTruthy();
  });
});
